const {
    SubmitOrder
} = require("./SubmitOrder.vue");

const {
    TestWatcher
} = require("jest");



describe('SubmitOrder.vue', () => {
    test('setup correctly', () => {
        expect(true).toBe(true);
    });
});
